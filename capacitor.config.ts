import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.charmee.mydeas',
  appName: 'Mydeas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
